package ru.white.utils.render;

public class MainMenuShaderRenderer implements DrawBatcher.Batched {
    public static final MainMenuShaderRenderer INSTANCE = new MainMenuShaderRenderer();
    public void flush() {}
}
